<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.maqson.notika">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="app.maqson.notika.SetupActivity" />
        <activity android:name="app.maqson.notika.DetailActivity"
            android:theme="@style/NoActionBarTheme" />
        <activity android:name="app.maqson.notika.SettingsActivity" />
        <activity
            android:name="app.maqson.notika.SignUpActivity"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name="app.maqson.notika.SignInActivity"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name="app.maqson.notika.ExploreActivity"
            android:theme="@style/NoActionBarTheme" />
        <activity
            android:name="app.maqson.notika.MainActivity"
            android:theme="@style/NoActionBarTheme" />
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
        <activity
            android:name="app.maqson.notika.SplashActivity"
            android:theme="@style/NoActionBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>